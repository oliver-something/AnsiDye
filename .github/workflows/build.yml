name: AnsiDye CMake Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        arch: [x86_64, x86]
    
    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v4

    # Install dependencies (if necessary)
    
    - name: Update
      run: sudo apt-get update
      
    - name: Install Build Essentials
      run: sudo apt install -y build-essential
    - name: Add PPA
      run: |
        sudo add-apt-repository ppa:ubuntu-toolchain-r/ppa -y
        sudo apt update
        sudo apt install -y g++-13 gcc-13
        sudo update-alternatives --install /usr/bin/gcc-13 60 --slave /usr/bin/g++-13
        sudo apt-get install -y gcc-13-multilib g++-13-multilib
    # Install CMake (if not already installed)
    - name: Install CMake
      run: sudo apt-get install -y cmake
    
    # Configure the CMake project with C++23 standard
    - name: Configure CMake with C++23
      run: cmake -B build -DCMAKE_CXX_STANDARD=23

    - name: Configure the CMake project
      run: cmake -B build

    - name: Build the project
      run: cmake --build build

    - name: Check executable
      run: test -x ./build/AnsiDye
